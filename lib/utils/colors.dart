import 'package:flutter/material.dart';

class CColors {
  static const primaryColor = Color(0xFF6C63FF);
  static const primaryLight = Color(0xFF8F89FF);
  static const secondaryColor = Color(0xFF63FFEF);
  static const secondaryLight = Color(0xFF89FFF7);
  static const accentColor = Color(0xFFFF6363);
  static const accentLight = Color(0xFFFF8989);

  // Prevent instantiation
  CColors._();
}
